import { Section } from '../components/section';
import './gallery.scss';

export const Gallery = () => {
    return (
        <Section>
            <section id="gallery">
                <h1>Gallery</h1>
                <p>This is a gallery of dogs</p>
            </section>
        </Section>
    );
};