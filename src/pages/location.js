import { Section } from '../components/section';
import './location.scss';

export const Location = () => {
    return (
        <Section>
            <section id="location">
                <h1>Location</h1>
                <p>This is the location information</p>
            </section>
        </Section>
    );
};