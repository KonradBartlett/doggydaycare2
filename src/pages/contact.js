import { Section } from '../components/section';
import './contact.scss';

export const Contact = () => {
    return (
        <Section>
            <section id="contact">
                <h1>Contact</h1>
                <p>This is the contact form</p>
            </section>
        </Section>
    );
};